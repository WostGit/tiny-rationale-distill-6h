# Agent Notes

- Keep scripts readable and CLI-driven.
- Prefer explicit JSON outputs over implicit logs.
- For this repository, `outputs/metrics/` is the canonical artifact root.
- This repository is a tiny reduced reproduction, not benchmark-grade.
